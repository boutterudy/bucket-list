<nav id="navbar">
    <div id="navbar-top">
        <ul>
            {% if app.user %}
                <li><a href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                <li><a href="{{ path('app_register') }}">Register</a></li>
                <li><a href="{{ path('app_login') }}">Login</a></li>
            {% endif %}
        </ul>
    </div>
    <div id="navbar-bottom">
        <p id="navbar-logo"><a href="{{ path('home') }}"><img src="{{ asset('assets/images/logo.png') }}" /> BucketList</a></p>
        <ul>
            <li><a href="{{ path('home') }}">Home</a></li>
            <li><a href="{{ path('about_us') }}">About us</a></li>
            <li><a href="{{ path('wish_list') }}">Wish list</a></li>
            {% if app.user %}
            <li><a href="{{ path('wish_create') }}">New wish</a></li>
            {% endif %}
        </ul>
    </div>
</nav>